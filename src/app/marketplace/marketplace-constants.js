'use strict';

module.exports = {
  // TEMPORARY: real marketplace URL replaced with a non-resolving placeholder.
  // Original: 'https://2vyd33gumd.execute-api.us-east-2.amazonaws.com/stage'
  // Restore the original before re-enabling the Store tab (FEATURE_FLAGS.STORE_TAB).
  DEFAULT_BASE_URL: 'https://fake-marketplace.invalid',
  DEFAULT_MARKETPLACE_ID: 1,
};
