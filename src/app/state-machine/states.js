'use strict';

const STATES = {
  IDLE:               'idle',
  WORKING:            'working',
  PLANNING:           'planning',
  WAITING_FOR_ACTION: 'waiting_for_action',
};

module.exports = { STATES };
